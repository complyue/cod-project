# This is a header comment
name: "test project"  # inline comment
version: 1.0

# Section comment
dependencies:
  - name: "dep1"
    version: "1.0"  # another comment
  - name: "dep2"
    version: "2.0"

# Final comment
config:
  debug: true
  timeout: 30 
