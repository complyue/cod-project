# Advanced YAML test
root:
  # Nested mapping
  nested:
    array: [1, 2, 3]
    mixed: {inline: true, count: 42}
  
  # Simple multiline (avoiding complex operators for now)
  description: "This is a simple multiline description"
  
  # Keep it simple for now
  folded: "This is a simple folded string"

# Final section
simple: true 
